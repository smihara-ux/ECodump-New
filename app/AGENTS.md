# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

The prototype must include working, separately rendered screens for every primary sidebar item. Keep all customer, worker, user, vehicle, address, phone, and project data anonymous.

## Durable visual direction

- Role architecture confirmed on 2026-09-25: support distinct construction, receiving, and driver experiences. Construction and receiving share soil matching; driver screens never include matching. Companies with both businesses switch construction/receiving mode without changing organization ownership. Keep condition agreement, reservation confirmation, trip completion, and receiving-result confirmation as separate states, and manage every vehicle rotation as a distinct trip.

- Receiving-screen direction confirmed on 2026-09-25: the main screen is the existing 搬出・受入スケジュール with 受入場所別 selected initially. Place it first in navigation and use it as the default entry and logo destination. Omit 労務安全 and 調整会議 from receiving navigation only; keep the existing construction-side destinations. Keep other functions and the existing schedule controls intact.

- Preserve every existing UX flow, route, control, label, table field, modal, search/filter behavior, and responsive interaction unless the user explicitly requests a UX change.
- Keep the overall layout and information density close to the current prototype; visual changes should feel like a restrained refresh rather than a new product.
- Use the supplied ECO DUMP logo consistently. Base the visual system on deep petroleum green, white, lime accents, and amber for warnings, with high-contrast readable tables.
- Sidebar visual target selected on 2026-08-25: compact operations navigation with framed outline icons, lime section headings and a pale-lime active row. Keep route destinations and interactions unchanged while using the display groups 現場業務・運行業務・基本台帳・関係会社 and the concise item labels shown in the selected mock.
- Product direction superseded on 2026-08-26: the selected source of truth is the ECO DUMP Transport Control Tower mock (`exec-b3f5e2fd-5bf4-4a90-8f3a-459727a01085.png`). The product must look independently designed at first glance: horizontal command bar, map-and-timeline home, dark industrial control-room palette, and a function launcher instead of the Buildee-like persistent sidebar. Preserve all established destination screens and core workflows through the launcher; modest navigation changes are explicitly acceptable.
- Navigation direction superseded on 2026-08-27: desktop uses the selected persistent left operations sidebar, tablets default to its compact icon rail, and phones use the same navigation as an off-canvas drawer. Keep the ECO DUMP control-room palette and existing routes/UX intact.
- Component direction selected on 2026-08-28: use the large lime primary buttons and framed utility icon buttons from the approved field-tablet mock. Provide persistent light/dark themes, minimum 44px touch targets, and preserve the same UX across Windows, macOS, iPhone, iPad, Android, and general tablet widths.
- Theme contrast direction confirmed on 2026-09-10: every control must remain legible before hover in both themes, with special attention to light mode. Use white or pale surfaces with deep-green text for secondary controls, dark text on lime primary actions, visible borders for inactive tabs and inputs, and readable muted text rather than low-opacity labels.

- Receiving implementation boundary (2026-09-25): receiving UI/state live in `src/receiving/` and `src/receiving-model.mjs`. Use six receiving menus, show destination-grouped schedule first, and keep driver unload reports separate from receiver confirmation. Every receiving mutation is explicitly an in-memory, unsent prototype; no API or DB save is claimed. Do not overwrite concurrent construction/driver work.

- Driver Mobile direction confirmed on 2026-09-25: present the driver prototype in an iPhone frame on desktop and at smartphone width on mobile. Bottom navigation is 今日の運行 / 運行履歴 / お知らせ / マイページ. Never expose soil matching or other-driver management. Preserve the admin work in progress; keep driver files isolated under `src/driver`. Clearly distinguish device-local drafts from API submission, keep unknown outcomes out of automatic retry, and never treat unloading as receiving-result confirmation.

- Shared direct-transport integration (2026-09-25): `?data=isolated` uses `src/integration`, `server/index.mjs`, and the isolated PostgreSQL validation DB. Keep this real-save boundary separate from existing in-memory/local driver prototypes. Preserve all registered migrations, including concurrent matching work, in `server/setup.mjs`; never remove another worker's migration entry. See `docs/SHARED_API_VALIDATION_2026-09-25.md` at repository root for verified scope and unresolved production Auth/device gates.

- Shared matching implementation (2026-09-25): the existing matching route now mounts `src/matching/SharedMatching.jsx`, backed by the same isolated PostgreSQL and validation sessions as `src/integration/ConnectedApp.jsx`. Keep four matching tabs, immutable offer/agreement snapshots, visibility-scoped documents, and separate agreement/requested-booking/confirmed-booking/trip states. `src/workflow/MatchingWorkflowPanel.jsx` and SQLite matching endpoints are older isolated experiments, not the active matching route. Never label the PostgreSQL matching screen API-unconnected or show sample fit/distance as calculated values.

- Construction sidebar switcher confirmed on 2026-09-25: in the pale-mint top area of the expanded construction sidebar, keep the existing ECO DUMP logo followed by 発生土マッチ, 施工側管理, and 総合インフォメーション（管理） as compact equal-height buttons. The matching entry belongs only in this top switcher and must not be duplicated in the construction vertical menu. Keep the collapse control on the row immediately below, aligned right. Matching and construction buttons use existing routes; until a real general-information route exists, disclose it as unimplemented instead of routing to an unrelated screen.
- Sidebar switcher polish confirmed on 2026-09-25: use a 336px expanded admin sidebar so the logo and three suite buttons have readable, balanced widths. Keep all buttons 76px high with consistent rounded corners and spacing; reserve a wider column for 総合インフォメーション（管理）, and retain the deep-green selected state with a clearly visible lime lower accent.

- iPhone presentation reference confirmed 2026-09-25: use a centered realistic black iPhone bezel on white canvas, rounded display, Dynamic Island with camera, decorative status indicators, and bottom home bar, matching the user's attached 10.38.37 screenshot. Keep ECO DUMP content and features. Both driver prototype and connected driver use the same frame, including narrow preview widths; status chrome is decorative, not actual device telemetry.

- Receiving sidebar switcher confirmed 2026-09-25: reuse the construction sidebar top strip with the existing logo, 発生土 / マッチ, 受入側 / 管理, and 総合 / インフォメーション / (管理), inside the mint sidebar area only. Remove matching from receiving vertical navigation but retain its route. Keep collapse on the next row and current responsive navigation. Management selection covers all receiving management pages; matching has its own selection. General information remains an explicit unimplemented dialog until a corresponding route exists. The reference image lower menus are not instructions to restore removed features.

- Matching search wording confirmed 2026-09-25: use 現場から探す and 受入れから探す for the two visible search choices. Preserve existing filtering and role defaults.

- Header update confirmed 2026-09-25: remove the visible 事業モード role selector from the shared admin header. Preserve role-specific routes and screens.

- Receiving navigation wording confirmed 2026-09-25: label the receiving home menu 受入管理. Preserve its existing schedule destination and receiving-location default.

- Matching entry correction confirmed 2026-09-25: normal matching opens the existing interactive screen immediately, without the development login or a collapsed sample section. Keep a concise unsaved/sample notice. Preserve shared API validation separately behind the explicit matchingApi=1 query; do not imply the normal preview saves to the DB.

- Construction navigation restoration confirmed 2026-09-25: keep the approved suite switcher and restore the grouped construction sidebar from the public gatekeeper-era baseline. Use the current map-and-timeline control screen as 運行ダッシュボード, keep 配車・運行管理 as a separate destination, restore existing construction service/master/partner routes, keep matching in the suite switcher only, and label the schedule destination 搬出管理.

- Approved admin restoration implemented 2026-09-25: preserve the published shell and approved top switcher. Receiving navigation groups are 受入業務 (受入管理 / 受入場所管理 / 搬入予約・受付), 運行管理 (運行ダッシュボード), 実績・設定 (実績・帳票 / 取引先・基本設定). Keep prior exclusions. Existing company/user/vehicle/partner/application/prime-contractor/gatekeeper screens remain reachable through settings; do not conflate worker gatekeeping with vehicle reception.
- Normal-page data integration supersedes the earlier preview-only entry boundary: anonymous preview is directly operable without a blocking developer login. The optional account dialog activates shared API data inside the same shell for matching, construction reservations/dispatch/results, and receiving home/reservations/results. Use authenticated server scopes; no automatic privileged login. Keep preview edits separate from persisted data. Location/master-data editing and the map remain explicitly sample/unconnected. Legacy SQLite panels are available only with workflowApi=1; matchingApi=1 retains the separate validation screen. Do not reinsert development panels above business screens.
- Construction operations refinement confirmed 2026-09-25: keep the field-first schedule home and expose today planned/completed/unassigned/not-departed/incomplete/slip-pending counts as detail filters, while distinguishing unique vehicles from trip count. Daily planning precedes prior-day/prior-week copy; copied plans remain drafts until date/count and conflict checks are acknowledged. Keep vehicle and driver masters separate, record substitutions with before/after/effective time once authority is settled, distinguish planned/driver-reported/receiver-confirmed results, and total t and m³ separately. Map routes are guidance candidates unless operationally approved, and every location must show freshness. Normal preview mutations must say local draft and must never claim persistence.
- Vehicle-master presentation confirmed 2026-09-25: keep 車両情報 as the single list and remove the separate 運転手情報 tab. Each vehicle row exposes an 運転手情報 action that opens the driver assigned to that vehicle. Preserve vehicle registration, search, and vehicle fields; do not restore a standalone driver list without a new explicit decision.
- Matching-detail visualization confirmed 2026-09-25: present the selected candidate's compatibility as a chart rather than a lone score. Keep soil, period, capacity, and document factors visible, retain workflow stages and route context, and explicitly label preview values as sample evaluation rather than a real calculation until the scoring API is connected.
- Matching capacity-chart direction confirmed on 2026-09-25: do not present period capacity as horizontal progress lines. Use vertical stacked bars for received, unfinished reservations, and free capacity, then connect each period's consumed total with a visible point-and-line trend. Preserve selectable period details, limit/overage semantics, separate m³/t controls, and the sample/unconnected disclosure.
- Operations-dashboard priority confirmed 2026-09-25: open with 本日の運行タイムライン as the main view. Selecting a trip switches to the map for that vehicle, distinguishes its travelled path from the remaining route, and makes in-transit, delayed, receiving, and on-site-waiting states immediately visible. Keep a clear control to return the timeline to the main view and preserve existing filters and summary totals.

- Receiving day operations confirmed 2026-09-25: keep the existing schedule date/search/location/congestion controls and sidebar. Within a receiving location, show origin-site summaries then a same-page dialog with vehicle/location, gate/photo, and receipt/quantity tabs; closing keeps list state and scroll. Count distinct vehicles separately from trips and exclude cancelled/rejected reservations from arrivals. `receivingSummary.mjs` defines displayed counts; confirmed receipt quantities are separate by original unit and never include pending or legacy unverified receipts.
- Receiving evidence uses scoped PostgreSQL `direct.receipts`, `receipt_history`, `gate_records`, `gate_history`, and `receive_mutate`, reusing existing receiving scope (no extra dispatch permission). Driver-submitted originals remain immutable. Arrival/unloading reports do not create gate times. Manual gate records start pending, duplicate detections may be dismissed, and duration requires exactly one confirmed entry and exit. Camera/GPS/OCR remain unconnected; physical capacity/monthly throughput/contract allowance and their subtraction rules remain undecided, so no calculated available capacity is claimed. General information is still unimplemented.
- Receiving operations dashboard confirmed 2026-09-25: default to the full-width 本日の運行タイムライン. Tapping a trip opens its map/status dialog while keeping the timeline filters and scroll. Provide shortcuts for inbound, waiting at origin, and receiving. Until trip-linked GPS history exists, sample paths/markers must be labeled as examples, never actual tracking. Keep construction dashboard layout unchanged.
- WINNERS Narita validation model confirmed 2026-09-25: manage the shared fictional IDs and rerunnable seed only in `server/setup.mjs` (`naritaIds`), with the ownership map in `../docs/narita-validation/COMMON_LOCATION.md`. Construction, receiving and driver sessions must use the same booking/trip rows and scoped accounts. Never imply that the fictional Narita airport sites or receiving locations are real contracts. Keep unsupported booking change/cancel, substitution history, issue reporting, shared GPS and one-booking/many-trip behavior visibly unconnected until their API/DB contracts exist.
- Capacity visualization confirmed 2026-09-25: receiving-location management and normal matching details show a capacity/quota bar chart with year/month selection, unit separation, limit/overage and clickable breakdown. Until shared capacity semantics are approved, keep it explicitly illustrative and disconnected from persisted bookings; never present it as actual available capacity.
- Capacity chart design A approved 2026-09-25: use the left proposal in exec-bbbd09cd-1304-402c-931e-c8b9be92e5a7.png. Deep-green control header, three large selected-period totals, vertical stacked monthly bars with visible scale and dashed limit, red overage caps (no trend line), selected-period breakdown, collapsed calculation explanation. Keep existing logo and explicit fictional-data boundary.
- Receiving shared-data v2 (2026-09-25): authenticated receiving pages use `ReceivingConnected` and `ReceivingLocationsConnected`; preserve the anonymous prototype separately. Shared contracts are `../docs/narita-validation/API_CONTRACT_V2.md`. Use trip IDs for row selection, receipt/gate mutations and attachments; bookings may repeat per trip. Conditions/history are shared DB data, while capacity subtraction and business-hour approval enforcement remain undecided/unconnected. Do not show a calculated available capacity. Preserve unit-separated confirmed aggregates, exclude cancelled unperformed trips from missing slips, and keep receipt photos private. Common SQL/API files are owned by the common/construction task; coordinate changes rather than overwriting them.
- Shared matching finish (2026-09-25): preserve the approved sidebar entry and connected matching design. Show previous-offer and agreement-snapshot/current-case differences without rewriting signed conditions. Bookings retain the shared match agreement FK; matching agreement never auto-confirms reservations or dispatch. Narita matching preview preparation creates/publishes shared cases only; formal end-to-end acceptance waits for user screen approval. See `../docs/narita-validation/MATCHING_COMPLETION.md`.
- General information (2026-09-25) supersedes the earlier unimplemented dialog: shared route `information` uses the logo-right button. Company/site audience checks reuse existing memberships/scopes. Dedicated information edit grants are empty by default; never infer platform editing from construction/receiving roles. Content taxonomy and grant administration remain proposals, documented in `../docs/information/IMPLEMENTATION.md`. Article publishing never sends email/Push. Explicit acknowledgements are version-specific, and unknown writes require lookup without automatic retry.

- Information access superseded 2026-09-25: construction and receiving suite buttons remain visible but disabled with identical dark styling in both themes. General information is operator-management-only. Business direct URLs must not mount the information component; business API sessions must not read articles/documents/operation history or mutate information. Keep existing content/functions for a separately authenticated operator surface; do not infer operator authority from business roles or editing grants. No operator surface/authentication is currently implemented.
