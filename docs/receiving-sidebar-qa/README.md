# 受入側サイドバー修正確認（2026-09-25）

承認画像の左側上部を基準に、施工側の既存ボタン領域を共通化。既存ロゴ／発生土マッチ／受入側管理／総合インフォメーション(管理)を淡いミントのサイドバー内に横配置した。

- 展開幅322px、ボタン高76px、文字13px。長いラベルも省略せず指定改行。選択中は深緑・白文字・ライム下線。
- 開閉ボタンを次の行の右端に配置。縦メニューのマッチ行を削除。他のメニューと業務機能は維持。
- マッチは既存ルート、受入側管理は既存ホームへ遷移。ホームの受入場所別初期表示を確認。
- 上部の選択状態はaria-currentで通知。キーボードTab・フォーカス枠・開閉を確認。
- 総合インフォメーションの対応ルートは未実装。既存ダイアログで未実装を明示し、別機能には接続しない。
- スマートフォンでは既存ドロワー方式を維持し、背景がクリックを遮らない重なり順へ修正。

ローカルChromiumで1440×1000、820×1180、390×844、360×800を確認。折りたたみ時は展開操作から上部ボタンへ到達可能。実機検証・本番公開は未実施。

[PC](pc.png) / [タブレット](tablet.png) / [スマートフォン](phone.png) / [360px](small-phone.png) / [測定と操作結果](evidence.json)

`app/scripts/check-receiving-sidebar.mjs`：4幅の配置、文字欠け、Tab、遷移、選択状態、未実装案内、開閉を確認。展開アニメーション終了後に撮影。

既存画面回帰：75件合格（regression.txt）。既存テストのマッチ入口を上部配置に対応させ、他担当の現行検索ラベル「現場から探す」に期待値を合わせた。ビルド成功（build.txt）。API・DB・認証は今回変更していない。
